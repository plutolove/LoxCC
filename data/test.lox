fn abs(a: i32) -> i32 {
  var ret:i32;
  if (a>0) {
    ret = a;
  } else {
    ret = -a;
  }
  return ret;
}

fn main(a: i32) -> i32 {
  var x: i32 = 23;
  var y:i32;
  y = x*4;
  abs(x*y);
  if (x>y) {
    var x:f32 = 34.45;
    y = x*y;
  } else {
    var y:f32 = 34.45;
    x = y+x;
  }
  return x*3;
}
